class StaticPagesController < ApplicationController
  def home
		@exercise = current_user.exercises.build if signed_in?
		@feed_items = current_user.feed.paginate(page: params[:page])  	
  end

  def help
  	@title="Help"
  end
end
